-- The T-SQL batch (DECLARE @cnt … IF @cnt = 0 INSERT …) became public.etl_pre_dim_customer()
-- converted with the sql-conversion skill: Pre SQL cannot hold variables or IF blocks (IC-10, IC-11).
SELECT public.etl_pre_dim_customer()
